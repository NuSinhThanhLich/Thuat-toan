#include <bits/stdc++.h>
using namespace std;

int main () {
	ios_base::sync_with_stdio(0); cin.tie(0);
	int n,a,b,c,d;
	cin >> n >> a >> b >> c;
	if (a <= b - c) {
		cout << n / a;
	} else {
		d = (n - b) / (b - c);
		d = d + 2;
		// mua dc 1 chai thuy tinh va mot chai nhua
		cout << d;
	}
}
